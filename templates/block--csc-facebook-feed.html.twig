{# block--csc-fb-feed.html.twig #}
{%
  set classes = [
  'block',
  'block-' ~ configuration.provider|clean_class,
  'block-' ~ plugin_id|clean_class,
  'csc-facebook-feed-block'
]
%}
<div{{ attributes.addClass(classes) }}>
  {{ title_prefix }}
  {{ title_suffix }}
  <div class="content">
    {{ markup }}
  </div>
</div>

